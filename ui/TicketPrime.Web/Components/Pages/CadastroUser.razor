@page "/cadastro-user"
@using System.Net.Http.Json
@rendermode InteractiveServer
@inject HttpClient Http
@inject NavigationManager Navigation

<h3>Cadastro de Usuário</h3>

<EditForm Model="@novoUsuario" OnValidSubmit="@HandleRegistration">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="mb-3">
        <label>Cpf:</label>
        <InputText @bind-Value="novoUsuario.Cpf" class="form-control" />
    </div>

    <div class="mb-3">
        <label>Nome:</label>
        <InputText @bind-Value="novoUsuario.Nome" class="form-control" />
    </div>

    <div class="mb-3">
        <label>Email:</label>
        <InputText @bind-Value="novoUsuario.Email" class="form-control" />
    </div>

    <button type="submit" class="btn btn-primary">Cadastrar</button>
</EditForm>

@if (!string.IsNullOrEmpty(mensagem))
{
    <p class="alert alert-info mt-3">@mensagem</p>
}

@code {
    // Substitua 'Usuario' pelo nome da sua classe de modelo
    private Usuario novoUsuario = new Usuario();
    private string mensagem = "";

    private async Task HandleRegistration()
    {

            // A "ROTA" que você perguntou: "api/usuarios"
      var response = await Http.PostAsJsonAsync("api/usuarios", novoUsuario);

        if (response.IsSuccessStatusCode)
        {
             mensagem = "Usuário cadastrado com sucesso!";
             novoUsuario = new Usuario(); // Limpa o formulário
        }
        else
        {
            mensagem = "Erro ao cadastrar: " + response.ReasonPhrase;
        }
    }
}